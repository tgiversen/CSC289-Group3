server {
    listen 80;

    # Serve static files from Vite build
    root /usr/share/nginx/html;
    index index.html;

    # React Router (always fallback to index.html)
    location / {
        try_files $uri /index.html;
    }

    # Proxy backend API
    location /api/ {
        proxy_pass http://backend:5000/api/;
    }
}
